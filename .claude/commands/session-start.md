# Session Start

Orient yourself before doing any work. Follow this reading order exactly.

1. Read `~/workspace/AGENTS.md` (global rules)
2. Read `AGENTS.md` in this project (project-specific context)
3. Run `git status` and `git log --oneline -10`
4. Find the latest file in `changelog/session-logs/` — read it
5. Check `docs/plans/` for any active task plans
6. Check `docs/research/` — skim the latest entry to refresh domain context
7. Check GitHub Issues (current Milestone) — what is in progress

Report to the user:
- What the last session accomplished (from session log)
- What appears to be in progress (from git status + plans)
- Any open questions or blockers carried forward

Do not start work until this orientation is complete.
