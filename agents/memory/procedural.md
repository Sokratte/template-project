# Procedural Memory

*Always-active rules and operator context — **loaded fully at every session
start**. Short by design. This is procedural memory: what the agent IS and
who it's working with, not what it looks up.*

*Strengthen on recall: if an entry prevents a mistake, update its tag —
today's date, counter +1 (`[YYYY-MM-DD xN]`). One edit.*

*Admission threshold for rules: constraints that apply to EVERY session,
regardless of topic. Domain-specific how-to belongs in operational memory.
Rules never decay automatically — the sweep does not touch this file.
Changes here are the operator's decision alone.*

---

## Operator profile

*Who you are working with and how. Stable — changes rarely, if ever.
Fill in during project setup as patterns emerge. A few lines is enough.*

- Designs by iterative pushback: floats an idea, expects it stress-tested,
  refines over several rounds before locking. Values being told plainly when
  an idea is weak, with a better alternative + rough probability. [2026-06-17 x1]
- Optimizes hard for simplicity / minimal surface area; resists extra files,
  folders, mechanisms unless they earn their place. "Maybe I'm
  over-engineering this" is an invitation to check, not reassurance. [2026-06-17 x1]
- Catches real bugs in proposed designs, often by reasoning from failure
  cost or from what an artifact is actually for (e.g. cloner-overwrite
  footgun; `##` vs `---` terminator; worklog doing two jobs) — engage the
  technical objection directly, don't smooth it over. [2026-06-17 x2]
- Specs/plans before implementation. Wants sign-off on each step when
  drafting; "write nothing without me signing off." [2026-06-17 x1]


## Procedural rules

<!-- Example entries — replace with this project's real rules:
- Before writing any file, verify it hasn't changed since you last read it.
  If the hash differs, stop and re-read. [YYYY-MM-DD x1]
- Verify every edit with git diff — tools can report success even when
  an anchor wasn't found and nothing changed. [YYYY-MM-DD x1]
- Update the worklog immediately when task state changes — not in a batch
  at session end. [YYYY-MM-DD x1]
-->
