<!-- keywords: tools, scripts, utilities, one-time, non-persistent -->
tools/README.md

# tools/ — Tools and scripts

Project-specific utility scripts live here. Scripts are run from the repo using bash only (`bash tools/myscript.sh`); never `chmod +x`. One-time or non-persistent tasks only — no install targets, no service starts.
