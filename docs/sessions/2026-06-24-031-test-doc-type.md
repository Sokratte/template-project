<!-- keywords: session, test doc type, TEST-001, doc-consistency suite, executable checks, session-end gate, PLAN-003, drift fixes -->
2026-06-24-031-test-doc-type.md

# Session 031 — Test document type & doc-consistency suite

**Status:** Completed
**Goal:** Design a test document type, build it, and integrate it into the workflow.

Following the manual hand-grepping that closed session 030, turn the recurring doc audit into an executable suite, add a lightweight TEST document type that registers suites, and gate session-end on it.

## Designed the two-artifact split and wrote PLAN-003 <!-- keywords: plan, design, boundary -->

Key decision: the TEST *document* registers a suite; the *executable code* in `tests/` does the testing. A markdown file that merely describes tests would be theatre. PLAN-003 captures goal, non-goals, design, checklist, acceptance.

## Built tests/test_docs.py — four checks, reads live tree <!-- keywords: suite, checks, verification -->

Checks: no legacy `kw:` header token, no pipe-form headers, content-doc line-1 keywords-comment + bare filename, no dead ADR/SPEC/PLAN/DP refs. Excludes `.trash/`; strips code/backtick spans so examples are not flagged. Verified by injecting each defect and confirming red, then restoring to green (acceptance criterion met). MCP venv lacks pytest — ran via direct import; suite is pytest-compatible for normal use.

## Registered the TEST type and TEST-001 <!-- keywords: docs README, types table, template, registry -->

Added TEST to `docs/README.md` (status-per-type Draft·Active·Superseded, types/location table, template block). Wrote `docs/tests/TEST-001-doc-consistency.md` (one content heading per check) and `docs/tests/README.md`.

## Wired session-end gate <!-- keywords: session-end, gate, step 7 -->

New `session-end.md` step 7 runs the suite before commit; red blocks close unless deferred as a `[FIND]`. Old steps 7/8 renumbered to 8/9.

## Drift the suite caught and fixed <!-- keywords: drift, session-028, session-030 -->

The suite immediately found two pre-existing defects: session-030 still had a pipe-form header (it was in the Set-B work committed separately, so the earlier keywords rename never touched it), and session-028 line 2 named the wrong file (`027-...` copy-paste). Both fixed.

## Open / carry-forward <!-- keywords: open, carry-forward -->

- [x] PLAN-003 executed end to end; suite green; gate live.
- [ ] Consider porting `tests/test_docs.py` to the other projects (TradingBot, tbh, …) once the template stabilises — the suite is repo-relative and should drop in.
- [ ] MCP server has no pytest in its venv; if suites grow, decide whether session-end runs them via the project venv instead.

## Git <!-- keywords: git, commit -->

Commits: cf2125c (feat: TEST doc type + suite). Earlier this session: 50175e9 (kw: → keywords: rename). Status: clean, pushed.
